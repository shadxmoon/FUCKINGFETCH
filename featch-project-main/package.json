{
  "name": "fetch-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
